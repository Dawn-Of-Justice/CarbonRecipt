/** Shared recharts tooltip styling so every dashboard chart matches. */
export const CHART_TOOLTIP_STYLE = {
  borderRadius: 12,
  border: "1px solid hsl(var(--border))",
  fontSize: 12,
  boxShadow: "0 8px 24px -12px rgba(12,73,50,0.25)",
} as const;
