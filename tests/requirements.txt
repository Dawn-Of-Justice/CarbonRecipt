# Test-suite dependencies for the Carbon Receipt contract tests.
# Install:  python -m pip install -r tests/requirements.txt
pytest>=7.0
requests>=2.28
httpx>=0.24
Pillow>=10.0   # used by fixtures/generate_receipts.py
